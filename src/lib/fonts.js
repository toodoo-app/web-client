import { Inter, Figtree } from "next/font/google";

export const inter = Inter({
    subsets: ['latin'],
    display: 'swap',
})

export const figtree = Figtree({
    subsets: ['latin'],
    display: 'swap'
})